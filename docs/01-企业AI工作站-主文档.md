# 企业 AI 工作站 · 主文档

> 版本：v2.1 · 2026-03-29 · 本文档是项目的核心设计基准，所有开发决策以此为准
> v2.1 变更：CTO终审修复8个BUG — AfterSale模型补齐(buyerCompensation/shopId/platform/platformAfterSaleId)、唯一约束修正、RefreshToken表新增、时区规范(UTC存储)、开发规范第9条
> v2.0 变更：9人团队审查 + CTO裁决通过的数据模型、权限体系、安全设计全面修订；AI双引擎策略对齐 ADR-011

---

## 一、项目定位

**不是普通工作台，是企业 AI 操作系统。**

核心价值：把数据、AI、业务流程三合一，让每个岗位的员工只需要"问"，不需要"操作"。未来所有员工只需拿着小程序登录，就能完成公司所有相关业务。

---

## 二、核心业务场景

### 现有能力
- 抖店、天猫、美团闪购等平台数据导入
- 多维度数据报告（推广率、达人、订单、气泡柱图表）
- 手工数据接入

### 近期目标
- 接入聚水潭 API，实现各店铺实时数据分析
- 各岗位 AI 工作助手（自然语言查询数据）

### 长期愿景
- 管理层：语音/文字问任意业务数据，实时汇总
- 财务：替代金蝶用友，自动生成报税数据
- 运营：投流建议、活动策划、达人数据分析
- 采购：供应商自动结算对账
- 员工从一周上班6天 → 3-4天，效率翻倍

---

## 三、产品形态

| 端 | 用途 | 优先级 |
|----|------|--------|
| 微信小程序 | 员工日常使用，随时随地 | P0 |
| Web 网页端 | 运营/财务深度使用 | P0 |
| 桌面客户端 Electron | 重度用户 | P2（二期） |

---

## 四、技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 后端 | Node.js + Express + Prisma | ERP SDK 生态最好 |
| 数据库 | PostgreSQL | 主数据库 |
| 缓存 | Redis | 会话 + 热数据缓存 |
| 前端 | Taro | 一套代码跑小程序 + H5 |
| AI（双引擎） | Claude Opus（驾驶舱+开发） + 百炼 Qwen（员工+客服） | 详见 ADR-011 双引擎策略 |
| 向量库 | pgvector | RAG 知识库 |
| 部署 | Docker Compose + 阿里云 | 已有服务器 |
| CI/CD | GitHub Actions | 自动化部署 |

---

## 五、多租户架构

**这是 SaaS 产品，未来要卖给其他企业，多租户从第一天设计进去。**

核心原则：
- 所有数据表必须包含 `tenant_id` 字段
- 所有数据库查询必须带 `tenant_id` 过滤
- 不同租户数据物理上在同一数据库，逻辑完全隔离
- 通过域名或企业码识别租户（如 `company.saas.com`）

---

## 六、数据模型

### 6.0 枚举定义

```prisma
enum Platform {
  douyin
  tmall
  meituan
  jd
  pinduoduo
}

enum OrderStatus {
  paid
  shipped
  completed
  refunded
  cancelled
}

enum RefundReason {
  quality_issue       // 质量问题
  wrong_item          // 发错货
  logistics_damage    // 物流损坏
  buyer_remorse       // 买家无理由退货
  not_as_described    // 与描述不符
  missing_item        // 缺件少件
  other               // 其他
}

enum Liability {
  supplier            // 供应商责任
  platform            // 平台责任
  logistics           // 物流责任
  buyer               // 买家责任
  shared              // 共同责任
}

enum SettlementStatus {
  draft
  confirmed
  paid
}

enum SyncType {
  incremental
  full
  manual
}

enum SyncStatus {
  running
  success
  failed
}

enum ToolCategory {
  health            // 健康/营养咨询类（如AIBI小酮营养师）
  data              // 数据类
  operation         // 运营类
  finance           // 财务类
  settlement        // 结算类
  marketing         // 营销类
  system            // 系统类
}
```

### 6.1 基础层（租户/用户/部门/权限）

```prisma
model Tenant {
  id               String    @id @default(uuid())
  name             String
  slug             String    @unique        // 域名前缀
  plan             String    @default("free") // free/basic/pro/enterprise
  planExpiresAt    DateTime?
  maxUsers         Int       @default(10)
  maxShops         Int       @default(5)
  aiQuotaMonthly   Int       @default(1000)
  status           String    @default("active") // active/suspended
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
  deletedAt        DateTime?
}

model Department {
  id        String    @id @default(uuid())
  tenantId  String
  name      String
  parentId  String?   // 支持多级部门树
  sortOrder Int       @default(0)
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deletedAt DateTime?

  @@index([tenantId, parentId])
}

model User {
  id             String    @id @default(uuid())
  tenantId       String
  phone          String
  name           String
  avatarUrl      String?
  wechatOpenid   String?
  wecomUserid    String?
  roleId         String
  departmentId   String?   // 所属部门，用于 dataScope=dept 数据隔离
  status         String    @default("active")
  lastLoginAt    DateTime?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  deletedAt      DateTime?

  @@unique([tenantId, phone])  // 同一手机号可在不同租户注册
}

// 用户-店铺关联表：运营角色"本店"权限需要知道用户负责哪些店铺
model UserShop {
  id       String @id @default(uuid())
  tenantId String
  userId   String
  shopId   String

  @@unique([tenantId, userId, shopId])
  @@index([tenantId, shopId])
}

model Role {
  id          String    @id @default(uuid())
  tenantId    String
  code        String    // super_admin/admin/finance/operation/procurement/viewer
  name        String
  permissions Json      // 权限矩阵 JSONB
  dataScope   String    @default("own") // all/dept/shop/own
  isSystem    Boolean   @default(false)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  deletedAt   DateTime?
}

// Refresh Token 存储表：支持多设备登录 + Token Rotation
model RefreshToken {
  id        String   @id @default(uuid())
  tenantId  String
  userId    String
  tokenHash String   // SHA-256 hash of refresh token（不存明文）
  deviceId  String?  // 设备标识（User-Agent hash 或自定义 device fingerprint）
  expiresAt DateTime // 7天有效期
  revokedAt DateTime? // 吊销时间（rotation 时旧 token 标记吊销）
  createdAt DateTime @default(now())

  @@index([tenantId, userId])
  @@index([tokenHash])        // 刷新时按 hash 查找
  @@index([expiresAt])        // 定时清理过期 token
}
```

### 6.2 业务层（店铺/订单/商品/供应商/结算/售后）

```prisma
model Shop {
  id              String    @id @default(uuid())
  tenantId        String
  name            String
  platform        Platform
  platformShopId  String?
  erpConnectionId String?
  syncStatus      String    @default("idle") // idle/syncing/error
  lastSyncedAt    DateTime?
  status          String    @default("active")
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  deletedAt       DateTime?

  @@index([tenantId, platform])
}

model ERPConnection {
  id           String    @id @default(uuid())
  tenantId     String
  erpType      String    // jushuitang/wangdiantong/guanyiyun/csv
  name         String
  credentials  Json      // AES-256-GCM 加密存储
  config       Json?
  syncInterval Int       @default(5) // 分钟
  status       String    @default("active")
  lastError    String?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  deletedAt    DateTime?
}

model Order {
  id              String       @id @default(uuid())
  tenantId        String
  shopId          String
  platformOrderId String
  status          OrderStatus
  totalAmount     Decimal      @db.Decimal(12,2)
  actualAmount    Decimal      @db.Decimal(12,2)
  refundAmount    Decimal      @default(0) @db.Decimal(12,2)
  buyerName       String?
  supplierId      String?
  items           Json         // 商品明细
  paidAt          DateTime?
  shippedAt       DateTime?
  completedAt     DateTime?
  rawData         Json?        // ERP 原始数据备份
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt
  deletedAt       DateTime?

  @@unique([tenantId, platformOrderId])  // 同租户下平台订单号唯一
  @@index([tenantId, shopId])
  @@index([tenantId, paidAt])
  @@index([tenantId, status])
  @@index([tenantId, supplierId])
}

model Product {
  id              String    @id @default(uuid())
  tenantId        String
  shopId          String
  skuCode         String
  name            String
  category        String?
  costPrice       Decimal   @db.Decimal(12,2)
  salePrice       Decimal   @db.Decimal(12,2)
  supplierId      String?
  stockQty        Int       @default(0)
  stockWarningQty Int       @default(50)
  status          String    @default("on_sale")
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  deletedAt       DateTime?

  @@unique([tenantId, shopId, skuCode])  // 同租户同店铺下SKU唯一
}

model Supplier {
  id            String    @id @default(uuid())
  tenantId      String
  name          String
  contactName   String?
  contactPhone  String?
  paymentCycle  String    @default("monthly") // weekly/biweekly/monthly
  bankInfo      Json?     // AES-256-GCM 加密存储
  returnPolicy  Json?
  status        String    @default("active")
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  deletedAt     DateTime?
}

model Settlement {
  id                  String           @id @default(uuid())
  tenantId            String
  supplierId          String
  shopId              String?          // 可按店铺维度结算
  periodStart         DateTime
  periodEnd           DateTime
  totalShipped        Int
  totalRefundQty      Int              @default(0)
  supplierFaultDeduct Decimal          @default(0) @db.Decimal(12,2)
  platformSubsidy     Decimal          @default(0) @db.Decimal(12,2)
  actualPayable       Decimal          @db.Decimal(12,2)
  detailSnapshot      Json             // 明细快照
  status              SettlementStatus @default(draft)
  confirmedAt         DateTime?
  paidAt              DateTime?
  createdAt           DateTime         @default(now())
  updatedAt           DateTime         @updatedAt
  deletedAt           DateTime?

  @@unique([tenantId, supplierId, periodStart, periodEnd])  // 同供应商同周期唯一
  @@index([tenantId, supplierId])
  @@index([tenantId, status])
}

model AfterSale {
  id                  String       @id @default(uuid())
  tenantId            String
  orderId             String
  shopId              String?      // 冗余店铺ID，便于按店铺维度查询售后
  platform            Platform?    // 冗余平台类型，便于按平台维度统计
  platformAfterSaleId String?      // 平台售后单号（同一订单同一SKU可能多次售后）
  skuCode             String
  supplierId          String?
  refundAmount        Decimal      @db.Decimal(12,2)
  reason              RefundReason
  liability           Liability
  platformSubsidy     Decimal      @default(0) @db.Decimal(12,2)
  buyerCompensation   Decimal      @default(0) @db.Decimal(12,2) // 买家补偿金额
  evidence            Json?        // 图片URL、截图等举证材料
  status              String       @default("pending") // pending/processing/completed/rejected
  createdAt           DateTime     @default(now())
  updatedAt           DateTime     @updatedAt
  deletedAt           DateTime?

  @@unique([tenantId, platformAfterSaleId])  // 同租户下平台售后单号唯一
  @@index([tenantId, orderId, skuCode])      // 按订单+SKU查询（原唯一约束降级为索引）
  @@index([tenantId, shopId])                // 按店铺维度查询售后
  @@index([tenantId, supplierId])
  @@index([tenantId, status])
  @@index([tenantId, createdAt])
}
```

### 6.3 AI & 系统层

```prisma
model Conversation {
  id             String    @id @default(uuid())
  tenantId       String
  userId         String
  agentType      String    // master/finance/operation/settlement/system
  title          String?
  contextSummary String?
  tokenUsed      Int       @default(0)
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  deletedAt      DateTime?

  @@index([tenantId, userId, createdAt])
}

model Message {
  id             String   @id @default(uuid())
  tenantId       String   // 所有表必须有 tenant_id
  conversationId String
  userId         String
  role           String   // user/assistant/tool
  content        String
  toolCalls      Json?
  toolResults    Json?
  createdAt      DateTime @default(now())

  @@index([conversationId, createdAt])
  @@index([tenantId, userId])
}

model CodeChange {
  id               String    @id @default(uuid())
  tenantId         String
  operatorId       String
  description      String
  diffContent      String
  testResult       Json?
  status           String    @default("pending") // pending/testing/approved/deployed/rolled_back
  deployedAt       DateTime?
  rollbackSnapshot String?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
  deletedAt        DateTime?
}

model Notification {
  id        String   @id @default(uuid())
  tenantId  String
  userId    String
  type      String   // alert/report/system
  channel   String   // miniprogram/wecom/sms
  title     String
  content   String
  isRead    Boolean  @default(false)
  sentAt    DateTime @default(now())

  @@index([tenantId, userId, isRead])
}

model AuditLog {
  id           BigInt   @id @default(autoincrement())
  tenantId     String
  userId       String
  action       String
  resourceType String
  resourceId   String?
  beforeData   Json?
  afterData    Json?
  ipAddress    String?
  createdAt    DateTime @default(now())

  @@index([tenantId, createdAt])
}

model DailyReport {
  id           String   @id @default(uuid())
  tenantId     String
  reportDate   DateTime
  targetRoleId String
  content      String
  dataSnapshot Json
  generatedAt  DateTime @default(now())

  @@index([tenantId, reportDate])
}

model SyncLog {
  id          String     @id @default(uuid())
  tenantId    String
  shopId      String?
  erpType     String     // jushuitang/wangdiantong/guanyiyun/csv
  syncType    SyncType   // incremental/full/manual
  status      SyncStatus // running/success/failed
  startedAt   DateTime   @default(now())
  finishedAt  DateTime?
  durationMs  Int?       // 耗时（毫秒）
  recordCount Int?       // 同步数据量
  errorDetail String?    // 错误详情
  createdAt   DateTime   @default(now())

  @@index([tenantId, createdAt])
  @@index([tenantId, status])
}
```

### 6.4 客服子系统（独立于工具市场，对标七鱼/美洽）

> **重要区分**：客服是核心子系统（有独立的 Customer Service Agent），不是工具市场中的工具。
> AIBI小酮是健康咨询类工具 Agent（营养师），不是客服。客服处理售后订单，小酮提供营养知识。

```prisma
// 客服渠道配置：每个渠道（微信小程序/淘宝/抖店）独立配置
model CustomerServiceChannel {
  id             String    @id @default(uuid())
  tenantId       String
  channelType    String    // wechat_miniprogram/taobao/douyin/jd/pdd
  name           String    // "自营小程序商城" / "淘宝旗舰店"
  config         Json      // 渠道连接配置（API Key、Webhook URL等），AES-256-GCM 加密
  shopId         String?   // 关联的店铺（可选，用于自动拉取订单数据）
  enabled        Boolean   @default(true)
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  deletedAt      DateTime?

  @@unique([tenantId, channelType, shopId])
  @@index([tenantId, enabled])
}

// 客服会话：一个用户一次咨询 = 一个会话
model CustomerServiceSession {
  id             String    @id @default(uuid())
  tenantId       String
  channelType    String    // 来源渠道
  channelId      String    // CustomerServiceChannel.id
  externalUserId String    // 外部用户标识（微信openid/淘宝buyerId等）
  buyerName      String?   // 买家昵称（脱敏）
  orderId        String?   // 关联订单ID（如有）
  category       String    @default("general") // general/after_sale/pre_sale/complaint
  status         String    @default("ai_handling") // ai_handling/human_queue/human_handling/resolved/closed
  assignedUserId String?   // 分配的人工客服（企业工作站用户ID）
  aiSummary      String?   // AI 生成的会话摘要（人工接手时使用）
  satisfaction   Int?      // 用户评分 1-5
  resolvedAt     DateTime?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@index([tenantId, status])
  @@index([tenantId, channelType, externalUserId])
  @@index([tenantId, assignedUserId, status])
  @@index([tenantId, createdAt])
}

// 客服消息：会话中的每条消息
model CustomerServiceMessage {
  id        String   @id @default(uuid())
  tenantId  String
  sessionId String
  sender    String   // buyer/ai/human_agent
  content   String   @db.Text
  msgType   String   @default("text") // text/image/file/card/system
  metadata  Json?    // 图片URL、文件信息、卡片数据等
  createdAt DateTime @default(now())

  @@index([sessionId, createdAt])
  @@index([tenantId, createdAt])
}

// 客服工单：需要人工处理的售后操作
model CustomerServiceTicket {
  id            String    @id @default(uuid())
  tenantId      String
  sessionId     String    // 关联客服会话
  orderId       String?   // 关联订单
  afterSaleId   String?   // 关联售后单（如已创建）
  type          String    // refund/exchange/return/complaint/other
  aiDecision    Json      // AI 判断结果（建议操作、退款金额、责任判定等）
  humanDecision Json?     // 人工最终决策
  status        String    @default("pending") // pending/approved/rejected/processing/completed
  processedBy   String?   // 处理的人工客服用户ID
  processedAt   DateTime?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  @@index([tenantId, status])
  @@index([tenantId, sessionId])
  @@index([tenantId, processedBy])
}
```

**客服子系统设计要点：**

- **API 资质模型（ADR-021）**：服务商/第三方平台一次申请，所有店铺（自有 + SaaS 租户）通过 OAuth 授权接入，无数量上限。与 ERP 适配器的 OAuth 模式完全一致，是 SaaS 核心壁垒
- **AI 处理 90%**：常见问题（物流查询、退换货政策、商品咨询）由 AI 自动回复，参考 RAG 知识库 + 实时查询 ERP 订单数据
- **人工确认 10%**：AI 无法处理或高风险操作（大额退款、批量退货、投诉升级）生成工单推送到企业工作站，人工客服在工作站确认处理
- **人机协作流程**：AI 处理中 → AI 无法解决 → 生成摘要 + 工单 → 推入人工队列 → 人工在工作站处理 → 结果回写 → 通知买家
- **渠道优先级**：Phase 2a 小程序商城客服（P0，微信第三方平台）→ Phase 2b 抖店飞鸽客服（P1，抖店服务商）→ 远期其他平台按需扩展。淘宝暂不接入（业务量小）
- **售后闭环**：AI 判定退款/换货 → 生成 AfterSale 记录草稿 → 人工确认 → 自动执行退款/创建退货单
- **品牌体验优先**：客服是品牌留存用户的关键触点，服务体验直接影响复购率，投入值得

### 6.5 工具层（工具市场/工具实例/知识库/用量日志）

```prisma
// 工具定义（系统级，所有租户共享工具目录）
model ToolDefinition {
  id           String   @id @default(uuid())
  code         String   @unique              // aibi-ketogenic, data-cleaner, ecom-assistant
  name         String                        // AIBI小酮
  description  String                        // 生酮营养师，1v1/群聊健康咨询
  icon         String?                       // 工具图标URL
  category     ToolCategory                  // 分类枚举
  version      String   @default("1.0.0")
  configSchema Json                          // 该工具可配置项的JSON Schema
  agentPrompt  String   @db.Text             // 该工具Agent的基础system prompt
  status       String   @default("active")   // active/deprecated
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

// 租户工具实例（每个租户独立配置，一个工具一个Agent）
model ToolInstance {
  id               String   @id @default(uuid())
  tenantId         String
  toolDefinitionId String
  name             String                    // 租户自定义名称
  enabled          Boolean  @default(false)
  config           Json                      // 人格设定、风格、阈值等（Agent读取）
  assignedRoles    Json                      // 哪些角色可使用/配置此工具
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  deletedAt        DateTime?

  @@unique([tenantId, toolDefinitionId])
  @@index([tenantId, enabled])
}

// 工具知识库文档（每个工具独立的知识库）
model ToolKnowledge {
  id             String   @id @default(uuid())
  tenantId       String
  toolInstanceId String
  title          String                      // 文档标题
  content        String   @db.Text           // 文档内容
  tokenCount     Int      @default(0)        // token数估算
  vectorized     Boolean  @default(false)    // 是否已向量化到pgvector
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  deletedAt      DateTime?

  @@index([tenantId, toolInstanceId])
}

// 工具使用日志
model ToolUsageLog {
  id             String   @id @default(uuid())
  tenantId       String
  toolInstanceId String
  userId         String?
  action         String                      // chat/config_update/knowledge_update/test
  inputTokens    Int      @default(0)
  outputTokens   Int      @default(0)
  latencyMs      Int      @default(0)
  createdAt      DateTime @default(now())

  @@index([tenantId, toolInstanceId, createdAt])
}
```

### 6.6 数据模型设计规范

- **多租户隔离**：所有表必须包含 `tenantId`，所有查询必须带 `tenantId` 过滤
- **软删除**：核心业务表均包含 `deletedAt DateTime?`，禁止物理删除
- **时间戳**：核心表均包含 `createdAt` + `updatedAt`，审计和消息等追加型表仅 `createdAt`
- **时区规范**：所有时间字段统一存储 **UTC 时间**，展示时按租户设置的时区（默认 Asia/Shanghai）转换。ERP 数据同步时将各平台本地时间转换为 UTC 后入库。前端请求日期范围参数统一使用 ISO8601 格式（如 `2026-03-29T00:00:00+08:00`），后端统一转 UTC 处理
- **金额精度**：所有金额字段统一 `Decimal(12,2)`，不使用 Float
- **枚举类型**：业务状态优先使用 Prisma enum，减少魔法字符串

---

## 七、角色权限体系

### 角色定义

| 角色 code | 名称 | 数据范围 dataScope | 特殊权限 |
|-----------|------|-------------------|---------|
| super_admin | 超级管理员 | all（全部） | 系统驾驶舱、代码部署 |
| admin | 管理员 | all（全部业务数据） | 用户管理 |
| finance | 财务 | dept（本部门财务数据） | 报税生成 |
| operation | 运营 | shop（本店数据，通过 UserShop 确定） | 投流分析 |
| procurement | 采购/对账 | dept（本部门供应商数据） | 结算确认 |
| viewer | 只读访客 | own（仅授权报表） | 无 |

### 权限级别说明

权限矩阵中的每个功能模块支持五级粒度控制：

| 权限级别 | code | 说明 |
|---------|------|------|
| 列表查看 | view | 查看列表和摘要信息 |
| 详情查看 | view_detail | 查看单条记录完整详情 |
| 编辑 | edit | 创建、修改数据 |
| 导出 | export | 导出数据为 Excel/CSV |
| 管理 | admin | 完全控制，包括删除和配置 |

### 权限矩阵

| 功能模块 | super_admin | admin | finance | operation | procurement | viewer |
|---------|-------------|-------|---------|-----------|-------------|--------|
| 全平台销售数据 | admin | admin | view/view_detail | view/view_detail（本店） | -- | -- |
| 财务流水/利润 | admin | view/view_detail | admin/export | -- | -- | -- |
| 报税数据生成 | admin | -- | admin/export | -- | -- | -- |
| 供应商信息 | admin | view | -- | -- | admin | -- |
| 结算单生成 | admin | view | -- | -- | admin/export | -- |
| 售后单管理 | admin | view/view_detail | view | edit（本店） | view/view_detail | -- |
| AI 对话 | admin（全域） | admin（全域） | edit（财务域） | edit（运营域） | edit（采购域） | -- |
| 系统驾驶舱 | admin | -- | -- | -- | -- | -- |
| ERP 配置 | admin | -- | -- | -- | -- | -- |
| 用户管理 | admin | edit | -- | -- | -- | -- |
| 数据导出 | export | export | export（财务） | export（本店） | export（结算） | -- |

### 数据隔离机制

- **Department（部门）**：当 `dataScope=dept` 时，用户只能访问其所属部门（`User.departmentId`）及下级部门的数据。Department 表支持 `parentId` 构建多级部门树
- **UserShop（用户-店铺关联）**：当 `dataScope=shop` 时（如运营角色），通过 UserShop 关联表确定用户负责的店铺范围，实现"本店"权限控制
- **数据过滤流程**：API middleware 根据当前用户的 `Role.dataScope` 自动注入过滤条件：
  - `all` → 仅过滤 `tenantId`
  - `dept` → 过滤 `tenantId` + 用户所属部门及下级部门
  - `shop` → 过滤 `tenantId` + UserShop 关联的 shopId 列表
  - `own` → 过滤 `tenantId` + `userId`

---

## 八、页面结构

### 小程序底部导航（按角色动态显示）

**超级管理员**：首页 / 店铺 / AI工作站 / 驾驶舱 / 我的（含工具入口）

**管理员**：首页 / 数据 / AI工作站 / 工具 / 我的

**财务**：工作台 / 流水 / AI助手 / 报税 / 我的

**运营**：工作台 / 店铺 / AI助手 / 活动 / 我的

**采购**：工作台 / 供应商 / AI助手 / 结算 / 我的

### 核心页面

```
/home          首页（各角色工作台）
/ai            AI 对话工作站（所有角色）
/shops         店铺管理
/finance       财务中心（财务专属）
/settlement    结算中心（采购专属）
/tools         工具市场
/cockpit       系统驾驶舱（super_admin 专属）
/users         用户管理
/settings      设置
```

### 系统驾驶舱（super_admin 专属入口）

功能：
- 与 Claude 对话，直接迭代系统功能
- 查看系统健康状态（API响应、数据库、同步状态）
- 代码变更预览 → 确认 → 一键部署
- 错误日志查询
- 一键回滚

安全机制：
- 每次进入需二次验证
- 写操作（代码修改/部署）必须用户确认，不自动执行
- 所有操作写入 AuditLog

---

## 九、数据同步策略

| 方式 | 频率 | 适用数据 |
|------|------|---------|
| 实时触发 | ERP Webhook | 订单状态变更、售后申请 |
| 增量同步 | 每5分钟 | 近24小时变更数据 |
| 全量同步 | 每天凌晨2点 | 所有历史数据 |
| 手动触发 | 按需 | 初始导入、数据修复 |

同步失败：指数退避重试（1分钟→5分钟→15分钟），全部失败后告警管理员。

每次同步操作均写入 `SyncLog` 表，记录同步类型、状态、耗时、数据量和错误详情，便于排查和监控。

---

## 十、部署架构

```
阿里云服务器
└── Docker Compose
    ├── nginx（统一入口 + HTTPS）
    ├── backend（Node.js API）
    ├── postgres（主数据库）
    ├── redis（缓存）
    └── worker（定时同步任务）
```

---

## 十一、项目目录结构（Monorepo，工作站+商城合一）

> ADR-022: 工作站和商城是一个项目，共享后端和数据库。商城是工作站的"手"。
> 时皙项目文档（/shishi/project-docs/）保留作为商城业务规则参考源。

```
enterprise-workstation/
├── packages/
│   ├── backend/                        # 统一后端（Express + Prisma）
│   │   ├── src/
│   │   │   ├── app.ts
│   │   │   ├── routes/
│   │   │   │   ├── employee/           # 员工工作站 API（/api/v1/employee/*）
│   │   │   │   │   ├── auth.ts
│   │   │   │   │   ├── shops.ts
│   │   │   │   │   ├── orders.ts
│   │   │   │   │   ├── ai-chat.ts
│   │   │   │   │   └── ...
│   │   │   │   ├── buyer/              # 买家商城 API（/api/v1/buyer/*）
│   │   │   │   │   ├── auth.ts         # 微信 openid 登录
│   │   │   │   │   ├── products.ts
│   │   │   │   │   ├── cart.ts
│   │   │   │   │   ├── orders.ts
│   │   │   │   │   ├── distribution.ts
│   │   │   │   │   └── ...
│   │   │   │   ├── admin/              # 商城管理 API（/api/v1/admin/*）
│   │   │   │   │   ├── products.ts
│   │   │   │   │   ├── orders.ts
│   │   │   │   │   └── distributors.ts
│   │   │   │   └── webhook/            # 回调（ERP/客服/微信支付）
│   │   │   │       ├── erp.ts
│   │   │   │       ├── cs.ts
│   │   │   │       └── wxpay.ts
│   │   │   ├── controllers/
│   │   │   ├── services/
│   │   │   │   ├── employee/           # 工作站业务逻辑
│   │   │   │   └── mall/               # 商城业务逻辑
│   │   │   ├── agents/                 # AI Agent
│   │   │   │   ├── master.ts
│   │   │   │   ├── finance.ts
│   │   │   │   ├── operation.ts
│   │   │   │   ├── settlement.ts
│   │   │   │   ├── customer-service.ts
│   │   │   │   ├── report.ts
│   │   │   │   └── system.ts
│   │   │   ├── tools/                  # 工具 Agent
│   │   │   │   ├── health/aibi-ketogenic/
│   │   │   │   └── ...
│   │   │   ├── adapters/
│   │   │   │   ├── erp/                # ERP 适配器
│   │   │   │   │   ├── base.ts
│   │   │   │   │   ├── jushuitang.ts
│   │   │   │   │   └── wangdiantong.ts
│   │   │   │   └── channels/           # 客服渠道适配器
│   │   │   │       ├── wechat-miniprogram.ts
│   │   │   │       ├── douyin-feige.ts
│   │   │   │       └── wecom.ts
│   │   │   ├── middleware/
│   │   │   │   ├── employeeAuth.ts     # 员工 JWT RS256
│   │   │   │   ├── buyerAuth.ts        # 买家微信 openid JWT
│   │   │   │   ├── tenant.ts
│   │   │   │   └── permission.ts
│   │   │   └── utils/
│   │   ├── prisma/
│   │   │   └── schema.prisma           # 统一 Schema（全部表）
│   │   └── package.json
│   ├── app-workstation/                # 员工端小程序（Taro + React）
│   │   ├── src/pages/
│   │   └── package.json
│   ├── app-mall/                       # 买家端小程序（Taro + React）
│   │   ├── src/pages/                  # ~22 个页面（时皙迁移）
│   │   └── package.json
│   └── shared/                         # 共享代码
│       ├── ui/                         # 共享 Taro 组件
│       ├── utils/                      # 通用工具函数
│       └── types/                      # 共享 TypeScript 类型
├── docs/
│   ├── brain.json                      # 项目大脑
│   └── 01~07 技术文档
├── deploy/
│   ├── docker-compose.yml
│   └── nginx/
├── pnpm-workspace.yaml
└── .env.example
```

**URL 路由分离**：同一个 Express 后端，按 URL 前缀区分身份
```
/api/v1/employee/*  → employeeAuth 中间件 → 工作站功能
/api/v1/buyer/*     → buyerAuth 中间件   → 商城买家功能
/api/v1/admin/*     → employeeAuth + admin 权限 → 商城管理功能
/webhook/*          → 签名验证           → ERP/客服/支付回调
/health             → 无认证             → 健康检查
```

---

## 十二、开发规范

1. **所有 API 响应统一格式**：`{ success, data, error?, meta? }`
2. **所有查询必须带 tenant_id**：在 middleware 层自动注入
3. **敏感字段加密**：bank_info、credentials 使用 AES-256-GCM（详见第十三节安全设计）
4. **操作审计**：财务操作、结算确认、代码部署必须写 AuditLog
5. **错误处理**：所有错误返回标准 error code，不暴露内部细节
6. **测试覆盖**：核心业务逻辑覆盖率 ≥ 80%
7. **软删除**：核心业务数据不可直接删除，统一使用 `deletedAt` 软删除字段
8. **输入校验**：所有 API 输入必须通过 Zod schema 校验
9. **时区统一**：所有时间存储 UTC，展示按租户时区转换，ERP 数据入库时转 UTC

---

## 十三、安全设计

### 13.1 认证方案（JWT）

| 配置项 | 值 | 说明 |
|-------|------|------|
| 签名算法 | RS256 | 非对称签名，公钥可安全分发给各服务验证 |
| Access Token 有效期 | 15 分钟 | 短期令牌，减少泄露风险 |
| Refresh Token 有效期 | 7 天 | 长期令牌，存储在 httpOnly cookie |
| Token 黑名单 | Redis SET | 用户登出、角色变更时将 token jti 加入黑名单 |
| 角色变更即时失效 | 是 | 管理员修改用户角色后，该用户所有已发 access token 立即失效 |

Token 刷新流程：
1. 客户端携带 access token 请求 API
2. access token 过期时返回 401
3. 客户端使用 refresh token 请求 `/auth/refresh`
4. 服务端用 SHA-256(refresh_token) 查询 `RefreshToken` 表，验证未过期、未吊销
5. 签发新 access token + 新 refresh token，旧 refresh token 标记 `revokedAt`（Rotation）
6. 若检测到已吊销的 refresh token 被复用（重放攻击），立即吊销该用户所有 refresh token

### 13.2 微信小程序登录流程

```
用户点击登录
  → wx.login() 获取 code
  → 前端将 code 发送到后端 /auth/wechat-login
  → 后端调用微信 code2session 接口换取 session_key + openid
  → 查询 User 表是否存在该 openid
    → 存在：直接签发 JWT（access token + refresh token）
    → 不存在：引导用户绑定手机号（wx.getPhoneNumber）
      → 后端解密手机号 → 创建 User 记录 → 签发 JWT
  → 返回 JWT 给前端，前端存入本地存储
```

注意事项：
- `session_key` 不可下发给前端，仅服务端保留用于解密
- 手机号解密使用微信提供的 AES-128-CBC 方案
- 首次登录后 `wechatOpenid` 字段写入 User 表，后续登录直接匹配

### 13.3 加密方案

| 项目 | 方案 | 说明 |
|------|------|------|
| 加密算法 | AES-256-GCM | 认证加密，同时保证机密性和完整性 |
| 密钥管理 | 阿里云 KMS | 主密钥托管在 KMS，应用层获取数据密钥（信封加密） |
| IV 生成 | 每次加密随机生成 12 字节 IV | 与密文一起存储，格式：`iv:ciphertext:authTag` |
| 加密字段 | ERPConnection.credentials、Supplier.bankInfo | 所有含密码、密钥、银行卡号的字段 |

### 13.4 CORS 策略

```javascript
const corsOptions = {
  origin: [
    'https://*.saas.com',           // 生产环境
    'https://servicewechat.com',    // 微信小程序
  ],
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH'],
  allowedHeaders: ['Content-Type', 'Authorization', 'X-Tenant-Id'],
  credentials: true,
  maxAge: 86400, // 预检缓存 24 小时
};
```

开发环境额外允许 `localhost:*`，通过环境变量控制。

### 13.5 输入校验

所有 API 输入必须通过 Zod schema 校验，在 controller 层入口统一执行：

```typescript
// 示例：创建订单的输入校验
const CreateOrderSchema = z.object({
  shopId: z.string().uuid(),
  platformOrderId: z.string().min(1).max(64),
  totalAmount: z.number().positive().multipleOf(0.01),
  items: z.array(z.object({
    skuCode: z.string(),
    qty: z.number().int().positive(),
    price: z.number().positive(),
  })).min(1),
});
```

规则：
- 字符串字段限制最大长度，防止存储攻击
- 数值字段限制范围，金额必须为正数
- 数组字段限制最大元素数量
- 校验失败返回 400 + 详细字段错误信息

### 13.6 Rate Limiting

| 限流维度 | 限制 | 说明 |
|---------|------|------|
| 租户级 | 100 req/min | 防止单个租户影响平台整体 |
| 用户级 | 30 req/min | 防止单用户滥用 |
| AI 对话接口 | 10 req/min（用户级） | AI 调用成本高，单独限流 |
| 登录接口 | 5 req/min（IP级） | 防止暴力破解 |

实现：基于 Redis 滑动窗口算法，超限返回 `429 Too Many Requests`。

---

## 十四、数据备份与灾备

### 14.1 备份目标

| 指标 | 目标值 | 说明 |
|------|-------|------|
| RPO（恢复点目标） | < 1 小时 | 最多丢失 1 小时数据 |
| RTO（恢复时间目标） | < 4 小时 | 4 小时内恢复服务 |

### 14.2 备份策略

| 备份类型 | 频率 | 保留时间 | 存储位置 |
|---------|------|---------|---------|
| WAL 归档 | 每小时 | 7 天 | 阿里云 OSS |
| 全量备份（pg_dump） | 每天凌晨 3 点 | 30 天 | 阿里云 OSS |
| 月度归档 | 每月 1 号 | 1 年 | 阿里云 OSS（低频存储） |

### 14.3 备份执行

```bash
# WAL 归档配置（postgresql.conf）
archive_mode = on
archive_command = 'ossutil cp %p oss://backup-bucket/wal/%f'

# 每日全量备份（crontab）
0 3 * * * pg_dump -Fc enterprise_workstation | ossutil cp - oss://backup-bucket/daily/$(date +\%Y\%m\%d).dump
```

### 14.4 恢复演练

- **频率**：每月一次
- **流程**：从 OSS 拉取最近备份 → 恢复到独立测试实例 → 验证数据完整性和业务查询 → 记录恢复耗时
- **演练结果**：记录到运维文档，恢复耗时超过 RTO 需启动优化
